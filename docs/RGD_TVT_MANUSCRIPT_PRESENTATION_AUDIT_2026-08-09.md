# RGD TVT 论文呈现审查

本文档仅追踪 `paper/main.pdf` 与 `paper/main.tex` 中尚未关闭的投稿前呈现问题，审查依据为 IEEEtran 双栏论文规范及论文引用实验工件的可核验性。

## 未关闭问题

| 优先级 | 发现 | 证据与影响 | 必要修正 |
|---|---|---|---|
| P1 | 门控过程可解释，但仅凭论文尚不能完全复现。 | 论文已固定 `H`、`gamma`、`kappa`、`T_A`、四个准入阈值和 `delta`，但尚未完整定义 `rho`、`T_t^{crit}`、`n_t^{pre}` 的构造或取值，以及 `d_t`、`h_t` 内部归一化分量和系数。 | 在第 III 节加入紧凑的参数/定义表，说明每个遗漏量的定义、单位或范围，以及其按帧重算还是按回合固定；实现细节可引用冻结协议。 |
| P1 | 跨论文情境表未在表内充分标注协议不可比性。 | 表 I、表 IV、表 V、表 VI 将 RGD 实测值和引用论文报告值置于同一表中。正文已有部分限定，但快速浏览时仍可能被误读为逐行的配对基准对比。 | 在表下注明：引用工作数值保留原论文的协议和指标定义，仅用于情境定位，并非同协议的直接对比。核心性能结论仍应由共同协议的配对实验承担。 |
| P1 | 图 7 的推断性数值需要正式溯源绑定。 | 图中各面板与正文叙述一致，但当前数组来自 `tmp/regenerate_figures_revision_20260808.py`；在正式结果目录中未定位到相应的已接受结果表或运行清单。 | 从带版本的正式 CSV/JSON 重新生成图 7，并在实验记录中绑定运行清单或哈希。在图中数值和置信区间可由该工件复现前，不应扩大其科学主张。 |

## 投稿前核查清单

1. 为缺失在线量补充紧凑的门控参数定义表或段落。
2. 为表 I、表 IV、表 V 和表 VI 增加情境对比说明。
3. 将图 7 绑定至已接受的正式结果工件，并从该工件重新生成图片。
